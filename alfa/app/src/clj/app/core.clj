(ns alfa.core
  "This ns mainly to create the production installer for zenpres-school app."
  (:require
    [alfa.installer.system :refer :all]
    [com.stuartsierra.component :as component]
    [alfa.grabber.component :as grab]))

(defn -main [& [args]] nil)
